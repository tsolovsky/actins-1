name: my first action

on: push

jobs:
    my-job:
        runs-on: ubuntu-latest
        steps:
            - name: my first action
              run: echo "my first acton"

            - name: list files
              run: ls
            
            - name: read file
              run: cat README.md
